# JARVIS AI STUDIO — variáveis de ambiente
# Nunca colocar chaves diretamente no código-fonte.

# --- MiniMax (adapter futuro, ver backend/app/engines/minimax) ---
MINIMAX_API_KEY=

# --- Cloud worker (arquitetura futura) ---
# RUNPOD_API_KEY=      # reservado para a próxima etapa (criação/destruição automática de Pods).
#                       # Nesta etapa (ligação manual a um Worker) NÃO é necessária —
#                       # testar /health não precisa de autenticação com o provider.
#                       # Nunca é enviada ao frontend, mesmo quando existir.
# TENSORDOCK_API_KEY=
# VAGON_API_KEY=
